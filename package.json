{
  "name": "node-express-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --exec babel-node src/index.js",
    "test": "echo \"No test specified\" && exit 0",
    "zip": "rm -f src/output.zip && rm -rf dist && npx mkdirp dist && cp -r node_modules dist && cp -r src dist && npx folder-zip-sync dist && cp -r output.zip src && rm output.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "nodemon": "^1.18.9"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "folder-zip-sync": "^1.0.5"
  }
}
